
                       ElasticSearch resCUE

Usage: escue [OPTIONS] [MANAGEMENT COMMAND] [COMMAND] [COMMAND OPTIONS]

Options:


Management Commands:
    cluster        Manage cluster.
    node           Manage node.
    index          Manage index.

Commands:
    change          change setting related with cluster, node, index.
    create          create cluster, node, index.
    install         install cluster, node, index.
    sync            synchronize files related with analysis.

Run 'escue COMMAND --help' for more information on a command.
