[node]

# Roles setting
node.roles=Enter roles:


# Paths setting
path.data=Enter data path:
path.logs=Enter logs path:

# Networking setting
http.host=Enter host:
http.port=Enter http port:
transport.port=Enter transport port:

# seed_hosts setting
discovery.seed_hosts=Enter seed hosts:

# Cluster initial master nodes
cluster.initial_master_nodes=Enter initial master nodes:

# Server setting
server.username=Enter user name:
install.path=Enter install path:


config.path=Enter config path:
jvm.heap=Enter heap size:

